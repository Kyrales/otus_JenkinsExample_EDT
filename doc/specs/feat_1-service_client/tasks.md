# Tasks

- [x] 1. Подготовить контекст реализации
  - Проверить актуальное состояние проекта `otusJenkinsExampleEDT` через `edt-mcp`.
  - Через инструменты EDT-MCP найти похожие паттерны документов, форм, отчетов и простой валидации записи.
  - Через инструменты EDT-MCP найти существующие паттерны YAxUnit-тестов в расширении `otusJenkinsExampleEDT.YAXUNIT`.
  - Убедиться, что справочник `Catalog.Контрагенты` доступен в основной конфигурации.
  - Убедиться, что подсистемы с именем `СервисноеОбслуживание` еще нет, либо определить безопасный способ доработки существующей.

- [x] 2. Создать подсистему сервисного обслуживания
  - Создать `Subsystem.СервисноеОбслуживание`.
  - Задать синоним **Сервисное обслуживание**.
  - Включить подсистему в командный интерфейс.
  - Проверить подсистему через EDT-MCP.

- [x] 3. Создать перечисление статусов
  - Создать `Enum.СтатусыЗаявокНаОбслуживание`.
  - Добавить значения `Новая`, `ВРаботе`, `Выполнена`, `Отменена`.
  - Задать русские синонимы перечисления и значений.
  - Включить перечисление в состав подсистемы `Subsystem.СервисноеОбслуживание` как связанный объект без отдельной пользовательской команды.
  - Проверить объект через EDT-MCP.

- [x] 4. Создать документ заявки
  - Создать `Document.ЗаявкаНаОбслуживание`.
  - Настроить документ как непроводимый.
  - Добавить реквизит `Контрагент` типа `CatalogRef.Контрагенты`.
  - Добавить реквизит `ОписаниеПроблемы` строкового типа.
  - Добавить реквизит `Статус` типа `EnumRef.СтатусыЗаявокНаОбслуживание`.
  - Добавить реквизит `КомментарийИсполнителя` строкового типа.
  - Разместить документ в подсистеме `Subsystem.СервисноеОбслуживание`.
  - Проверить метаданные документа через EDT-MCP.

- [x] 5. Настроить форму документа
  - Создать или скорректировать основную форму документа.
  - Разместить на форме поля `Контрагент`, `ОписаниеПроблемы`, `Статус`, `КомментарийИсполнителя`.
  - Сделать поля описания проблемы и комментария удобными для многострочного ввода.
  - Реализовать заполнение статуса `Новая` для нового объекта на форме, чтобы значение было видно пользователю до записи.
  - Проверить структуру формы через `get_form_layout_snapshot`, если инструмент доступен.

- [x] 6. Реализовать заполнение статуса и валидацию
  - Через EDT-MCP найти существующие паттерны обработчиков записи документов.
  - В модуле объекта документа продублировать установку статуса `Новая`, если статус не заполнен перед записью.
  - В модуле объекта документа реализовать проверку заполнения `Контрагент`.
  - В модуле объекта документа реализовать проверку заполнения `ОписаниеПроблемы`.
  - При ошибке заполнения останавливать запись и выводить понятное сообщение пользователю.
  - Записывать BSL только через EDT-MCP.

- [x] 7. Создать отчет по заявкам
  - Создать `Report.ЗаявкиНаОбслуживание`.
  - Реализовать простой СКД-отчет с одним набором данных-запросом.
  - Включить в результат поля `Номер`, `Дата`, `Контрагент`, `ОписаниеПроблемы`, `Статус`.
  - Настроить сортировку по дате по убыванию, если это не усложняет MVP.
  - Не добавлять сложные параметры, группировки и аналитику в MVP.
  - Разместить отчет в подсистеме `Subsystem.СервисноеОбслуживание`.
  - Проверить запрос отчета через `validate_query`, когда документ уже создан; для СКД использовать режим проверки СКД.

- [x] 8. Провести техническую проверку
  - Запустить `get_problem_summary` для проекта `otusJenkinsExampleEDT`.
  - При необходимости получить детали ошибок по новым объектам.
  - Убедиться, что новые объекты не создают ошибок EDT.
  - Проверить, что документ не требует проведения и не формирует движения по регистрам.
  - Проверить, что документ и отчет доступны через подсистему **Сервисное обслуживание**.

- [x] 9. Добавить простые YAxUnit-тесты
  - Использовать существующее расширение `otusJenkinsExampleEDT.YAXUNIT` из `src/cfe/otusJenkinsExampleEDT.YAXUNIT`.
  - Размещать в расширении только тестовый код, без продуктивной логики фичи.
  - Создавать тестовые данные внутри тестов или использовать существующий fixture, если такой паттерн уже есть в расширении.
  - Использовать уникальные наименования тестовых данных, чтобы тесты не зависели от ручного наполнения базы.
  - Добавить тест, что новая заявка без явно заданного статуса получает статус `Новая`.
  - Добавить тест, что заявка без контрагента не записывается.
  - Добавить тест, что заявка без описания проблемы не записывается.
  - Добавить тест, что корректная заявка с контрагентом и описанием записывается.
  - Через `list_configurations` определить launch-конфигурацию для запуска тестов.
  - Запустить и проверить тесты через `run_yaxunit_tests` с фильтром по расширению `otusJenkinsExampleEDT.YAXUNIT`.

- [x] 10. Проверить пользовательский сценарий вручную
  - [x] Обновить информационную базу после изменения метаданных.
  - [x] Открыть подсистему **Сервисное обслуживание** и убедиться, что из нее доступно создание заявки.
  - [x] Создать заявку без контрагента и убедиться, что запись остановлена.
  - [x] Создать заявку без описания проблемы и убедиться, что запись остановлена.
  - [x] Создать корректную заявку и убедиться, что статус по умолчанию `Новая`.
  - [x] Изменить статус заявки вручную.
  - [x] Открыть отчет из подсистемы **Сервисное обслуживание** и убедиться, что сохраненная заявка отображается.
  - Примечание: проверка выполнена через `onec-mcp-toolkit`: подсистема включена в командный интерфейс и содержит `Документ.ЗаявкаНаОбслуживание` и `Отчет.ЗаявкиНаОбслуживание`; формы документа открываются в клиентском контексте; форма отчета открывается как `Отчет.ЗаявкиНаОбслуживание.Форма`; отрицательные сценарии записи остановлены; созданные ранее заявки отображаются при чтении данных.

- [x] 11. Сверить результат с требованиями
  - [x] Проверить Requirement 1: заявка создается, сохраняется и доступна для просмотра.
  - [x] Проверить Requirement 2: статусы ограничены перечислением и значение по умолчанию работает.
  - [x] Проверить Requirement 3: обязательные поля контролируются.
  - [x] Проверить Requirement 4: отчет показывает сохраненные заявки.
  - [x] Проверить Requirement 5: YAxUnit-тесты покрывают ключевую серверную валидацию документа.
  - [x] Проверить Requirement 6: документ и отчет доступны в подсистеме **Сервисное обслуживание**.
  - [x] Зафиксировать, что out of scope не был реализован сверх MVP.
  - Примечание: `onec-mcp-toolkit` подтвердил, что подсистема входит в командный интерфейс, содержит документ и отчет, а форма отчета открывается в клиентском контексте.

- [x] 12. Наполнить тестовыми примерами через 1C MCP Toolkit
  - [x] Проверить доступность 1C MCP Toolkit REST API: `http://localhost:6003/health` вернул `status=healthy`, `mode=native`, `port=6003`.
  - [x] Проверить в живой базе наличие метаданных `Документ.ЗаявкаНаОбслуживание`, `Справочник.Контрагенты`, `Перечисление.СтатусыЗаявокНаОбслуживание`.
  - [x] Проверить наличие исходных контрагентов для тестовых заявок: найдены существующие элементы справочника `Контрагенты`.
  - [x] Создать тестовые заявки с префиксом `MCP_AUTO_20260614_`.
  - Результат создания: создано 4 заявки через `execute_code` 1C MCP Toolkit REST API.
  - Созданные документы:
    - `000000001` — контрагент `Животноводство ООО`, описание `MCP_AUTO_20260614_1 - тестовая заявка: Статус по умолчанию`, статус `Новая`.
    - `000000002` — контрагент `Корнет ЗАО`, описание `MCP_AUTO_20260614_2 - тестовая заявка: В работе`, статус `В работе`.
    - `000000003` — контрагент `Магазин "Бытовая техника"`, описание `MCP_AUTO_20260614_3 - тестовая заявка: Выполнена`, статус `Выполнена`.
    - `000000004` — контрагент `Магазин "Мясная лавка"`, описание `MCP_AUTO_20260614_4 - тестовая заявка: Отменена`, статус `Отменена`.
  - Проверка результата выполнена через объектную модель 1С в `execute_code`: найдено 4 заявки, первая заявка получила статус по умолчанию `Новая`.
  - Примечание: прямой `execute_query` по реквизиту `ОписаниеПроблемы` вернул ошибку доступности поля, поэтому контроль созданных данных выполнен чтением объектов документа.
